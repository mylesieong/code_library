工作表1
	Conda	List packages	conda list
	Conda	list environment/ chk out current env	conda env list
	Conda	Move to an env	(on bash) source activate {env_name}
	Conda	Move to an env	(on bash) source deactivate {env_name}
	Conda	Create an Env	conda create -n {env_name} {needed_package_1} {needed_package_2}
e.g. conda create -n my_env numpy
	Conda	Install package	conda install numpy
	/* move the python 101 here */


工作表2


工作表3


